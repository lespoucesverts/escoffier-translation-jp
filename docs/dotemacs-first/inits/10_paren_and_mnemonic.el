;;対応するかっこを光らせる
(show-paren-mode 1)

;; タブにスペースを使用する
(setq-default tab-width 4 indent-tabs-mode nil)

;;改行コード表示
(setq eol-mnemonic-dos "(CRLF)")
(setq eol-mnemonid-mac "(CR)")
(setq elo-mnemonic-unix "(LF)")

;;列数表示
(column-number-mode)
